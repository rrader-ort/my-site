function setup() {
    createCanvas(600, 400);
    background(200);
}

function draw() {
    background(200);
    fill(150, 0, 150);
    ellipse(mouseX, mouseY, 50, 50);
} 

// тепер у вас є підказки за допомогою Github Copilot.

function mousePressed() {
    // підтвердити запропонований код можна за допомогою клавіші Tab
    console.log('Hello, world!');
}

// це код на p5.js, який ви можете змінювати. тут ми пишемо наш власний проєкт.